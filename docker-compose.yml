version: "2.1"
services:
  xserver:
    build: ./
    restart: always
    privileged: true
    network_mode: host
  xeyes:
    build: ./example/xeyes
    restart: always
    privileged: true
    network_mode: host
    depends_on:
      - xserver
