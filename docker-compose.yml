version: "2.1"
volumes:
  display:
services:
  display:
    build: ./
    restart: always
    privileged: true
    volumes:
      - 'display:/tmp/.X11-unix'
  xeyes:
    build: ./example/xeyes
    restart: always
    volumes:
      - 'display:/tmp/.X11-unix'
